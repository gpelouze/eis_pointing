description: >
  Search for a translation in X and Y, with sub-pixel precision,
  and using explicit cross-correlation computation (ie. no FFT),
  and dropping values from non-overlaping regions.
steps:
  - type: shift
    cc_function: explicit
    cc_boundary: drop
    sub_px: True
